"""JV-Data のコード値デコード表。

JV-Data 仕様書 (`.refarence/.../ドキュメント/JV-Data仕様書_*.pdf`) のコード表に基づく。
ビューアで表示するのは日本語ラベルなので、加工段階でデコードして JSON に埋め込む。

TODO: 仕様書のコード表を確認し、網羅的に埋める。以下はよく使う値の暫定。
"""

# 性別コード (SexCD)
SEX_CD: dict[str, str] = {
    "1": "牡",
    "2": "牝",
    "3": "セン",
}

# 毛色コード (KeiroCD) — 代表的な値のみ。TODO: 仕様書で全コードを確認して補完。
KEIRO_CD: dict[str, str] = {
    "01": "栗毛",
    "02": "栃栗毛",
    "03": "鹿毛",
    "04": "黒鹿毛",
    "05": "青鹿毛",
    "06": "青毛",
    "07": "芦毛",
    "08": "栗粕毛",
    "09": "鹿粕毛",
    "10": "青粕毛",
    "11": "白毛",
}


def decode_sex(code: str) -> str:
    """性別コードを日本語ラベルへ。未知コードは空文字。"""
    return SEX_CD.get((code or "").strip(), "")


def decode_keiro(code: str) -> str:
    """毛色コードを日本語ラベルへ。未知コードは空文字。"""
    return KEIRO_CD.get((code or "").strip(), "")
